<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#1AFFFFFF">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Изображение товара -->
        <ImageView
            android:id="@+id/productImage"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder_image"
            android:background="#2A2A2A" />

        <!-- Название товара -->
        <TextView
            android:id="@+id/productName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/productImage"
            android:layout_marginStart="16dp"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Цена -->
        <TextView
            android:id="@+id/productPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/productImage"
            android:layout_below="@id/productName"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:textColor="#4169E1"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Характеристика 1 -->
        <TextView
            android:id="@+id/memorySpec"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/productImage"
            android:layout_below="@id/productPrice"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:textColor="#AAAAAA"
            android:textSize="12sp" />

        <!-- Характеристика 2 -->
        <TextView
            android:id="@+id/clockSpec"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/memorySpec"
            android:layout_below="@id/productPrice"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            android:textColor="#AAAAAA"
            android:textSize="12sp" />

        <!-- Характеристика 3 -->
        <TextView
            android:id="@+id/connectorsSpec"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/clockSpec"
            android:layout_below="@id/productPrice"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            android:textColor="#AAAAAA"
            android:textSize="12sp" />

        <!-- Кнопки админа (в правом верхнем углу) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Кнопка редактирования -->
            <TextView
                android:id="@+id/editButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:gravity="center"
                android:text="✏️"
                android:textSize="16sp"
                android:background="@drawable/circle_background_edit"
                android:layout_marginEnd="8dp"
                android:clickable="true"
                android:focusable="true" />

            <!-- Кнопка удаления -->
            <TextView
                android:id="@+id/deleteButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:gravity="center"
                android:text="🗑️"
                android:textSize="16sp"
                android:background="@drawable/circle_background_delete"
                android:clickable="true"
                android:focusable="true" />

        </LinearLayout>

        <!-- Кнопка добавления в корзину (скрыта для админа) -->
        <Button
            android:id="@+id/addToCartButton"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_below="@id/connectorsSpec"
            android:layout_marginTop="8dp"
            android:text="В КОРЗИНУ"
            android:textSize="12sp"
            android:backgroundTint="#4169E1"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:visibility="gone" />

    </RelativeLayout>
</androidx.cardview.widget.CardView>